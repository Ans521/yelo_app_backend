/**
 * JWT secret used for signing and verifying tokens.
 * Only from code – not from .env.
 */
export const JWT_SECRET = '102312jkds112';

/**
 * Admin panel credentials (no DB). Same JWT_SECRET for admin token.
 */
export const ADMIN_EMAIL = 'yelo@gmail.com';
export const ADMIN_PASSWORD = 'pp';
